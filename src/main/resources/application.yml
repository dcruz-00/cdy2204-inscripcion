cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
      session-token: ${AWS_SESSION_TOKEN}
    region:
      static: us-east-1
    stack:
      auto: false